# UCC Chatbot Assistant Environment Configuration
# Copy this file to .env and update values before deployment.
# NEVER commit the real .env file (it's in .gitignore).

# Server
PORT=8081

# Database - PostgreSQL (Netlify DB) or MySQL
# === Option A (VERIFIED LIVE): Netlify PostgreSQL ===
# Connection is used and verified: 2026-09-02
# Host: ep-lucky-mouse-a5nt5hhl.us-east-2.db.netlify.com
# Database: netlifydb
# Username: netlifydb_owner
DB_TYPE=postgres
DB_URL=jdbc:postgresql://ep-lucky-mouse-a5nt5hhl.us-east-2.db.netlify.com:5432/netlifydb?sslmode=require
DB_USERNAME=netlifydb_owner
DB_PASSWORD=npg_FyursVz6IBg8
DB_DRIVER=org.postgresql.Driver
DB_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# === Option B: Local MySQL (uncomment to use) ===
# DB_TYPE=mysql
# DB_URL=jdbc:mysql://localhost:3306/ucc_chatbot_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
# DB_USERNAME=root
# DB_PASSWORD=your-mysql-password
# DB_DRIVER=com.mysql.cj.jdbc.Driver
# DB_DIALECT=org.hibernate.dialect.MySQLDialect

# === Option C: H2 in-memory (for quick local dev) ===
# DB_TYPE=h2
# DB_URL=jdbc:h2:mem:uccchatbot;DB_CLOSE_DELAY=-1
# DB_USERNAME=sa
# DB_PASSWORD=
# DB_DRIVER=org.h2.Driver
# DB_DIALECT=org.hibernate.dialect.H2Dialect

# Optional: wipe and recreate schema on next boot (DANGEROUS - drops ALL tables)
# DB_RESET_ON_BOOT=true

# AI Configuration
# The chatbot has a built-in self-contained static UCC knowledge base
# (EN + SW) that handles all common questions even without any external LLM.
#
# Optionally enable an LLM for natural-language rephrasing by setting
# AI_PROVIDER to one of the supported providers and supplying a key.
# If AI_PROVIDER is left blank or unknown, the chatbot silently falls back
# to the static KB and answers every verified UCC question.
#
# Supported providers:
#   - "openai"   — OpenAI or any OpenAI-compatible endpoint (BYO key)
#   - "pollinations" — DEPRECATED: Pollinations.ai no longer offers a free
#                      anonymous tier as of 2026; their public key 402s.
#                      Only useful if you have a `sk_` API key from
#                      https://enter.pollinations.ai
AI_PROVIDER=
AI_API_KEY=
AI_API_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini
AI_EMBEDDING_MODEL=text-embedding-3-small

# JWT - MUST set a strong random secret in production
# Generate with: openssl rand -base64 48
JWT_SECRET=icmjsOTTJ4c70d2RxxavpWG1ajTMADdI0uCflQ/559gH6mRcYpZ8vPNsg8fGp4dn
JWT_EXPIRATION_MS=604800000

# Encryption key for stored API secrets (AES-256-GCM).
# MUST be set to a 32+ char random string in production. If left empty or
# left at the example default, the app will refuse to encrypt/decrypt any
# stored secrets (fail-closed).
# Generate with: openssl rand -base64 48
ENCRYPTION_KEY=

# Admin User - DataLoader seeds this user on first run
ADMIN_EMAIL=admin@ucc.co.tz
ADMIN_PASSWORD=ChangeMeOnFirstLogin!
ADMIN_NAME=UCC Administrator

# CORS (add your frontend origins)
CORS_ALLOWED_ORIGINS=http://localhost:5500,http://localhost:3000,http://localhost:3001,http://localhost:8080,http://localhost:8081,https://uccchatbot.netlify.app,https://master--uccchatbot.netlify.app

# Frontend URL
FRONTEND_URL=https://uccchatbot.netlify.app
