name = "ucc-chatbot"
main = "workers/index.js"
compatibility_date = "2024-09-23"
workers_dev = true

[assets]
directory = "frontend"

# Cloudflare Pages deployment is handled by .github/workflows/deploy.yml
# The Worker proxy below catches /api/* requests that are not static assets.
